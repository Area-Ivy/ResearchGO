# 🚀 ResearchGO 快速启动指南

## 一键启动整个系统

### 前置要求

- Docker Desktop（已启动）
- 至少 8GB 可用内存
- OpenAI API Key

### 步骤

#### 1. 配置环境变量

```bash
# 复制环境变量示例文件
cp env.example .env

# 编辑 .env 文件，填写你的 OpenAI API Key
# OPENAI_API_KEY=sk-your-key-here
```

#### 2. 一键启动

```bash
# 构建并启动所有服务
docker-compose up -d --build

# 查看启动日志
docker-compose logs -f
```

#### 3. 启动前端（单独）

```bash
cd frontend
npm install
npm run dev
```

#### 4. 访问服务

| 服务 | 地址 | 说明 |
|------|------|------|
| **前端应用** | http://localhost:5173 | Vue.js 开发服务器 |
| **API 网关** | http://localhost:8080 | Traefik 统一入口 |
| **Traefik Dashboard** | http://localhost:8081 | 路由监控 |
| **MinIO Console** | http://localhost:9001 | 对象存储管理 |
| **Attu (Milvus UI)** | http://localhost:9002 | 向量数据库管理 |
| **RabbitMQ 管理** | http://localhost:15672 | 消息队列管理 |

## 服务架构

```
    ┌─────────────────┐
    │    Frontend     │  (npm run dev)
    │   (Port 5173)   │
    └────────┬────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│                     Traefik API Gateway                        │
│                        (Port 8080)                             │
└────────────────────────────────────────────────────────────────┘
        │           │           │           │           │
        ▼           ▼           ▼           ▼           ▼
┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐
│   Agent   │ │   Auth    │ │  Paper    │ │  Vector   │ │Literature │
│  :8000    │ │  :8001    │ │  :8003    │ │  :8004    │ │  :8005    │
└───────────┘ └───────────┘ └───────────┘ └───────────┘ └───────────┘
        │           │           │           │           │
        └───────────┴───────────┴─────┬─────┴───────────┘
                                      │
        ┌─────────────┬───────────────┼───────────────┬─────────────┐
        ▼             ▼               ▼               ▼             ▼
  ┌──────────┐  ┌──────────┐   ┌──────────┐   ┌──────────┐  ┌──────────┐
  │  MySQL   │  │  MinIO   │   │  Milvus  │   │  Redis   │  │ RabbitMQ │
  │  :3306   │  │  :9000   │   │  :19530  │   │  :6379   │  │  :5672   │
  └──────────┘  └──────────┘   └──────────┘   └──────────┘  └──────────┘
```

## 常用命令

```bash
# 启动所有服务
docker-compose up -d

# 停止所有服务
docker-compose down

# 重启单个服务
docker-compose restart agent-service

# 查看服务状态
docker-compose ps

# 查看服务日志
docker-compose logs -f agent-service

# 重新构建并启动
docker-compose up -d --build

# 清理并重新开始
docker-compose down -v
docker-compose up -d --build
```

## 开发模式

如果只想启动基础设施，微服务在本地开发：

```bash
# 只启动基础设施
docker-compose up -d mysql minio etcd milvus redis rabbitmq

# 本地启动微服务
cd backend/services/agent-service
pip install -r requirements.txt
python run.py
```

## 服务端口对照表

| 服务 | 容器端口 | 宿主机端口 |
|------|---------|-----------|
| Traefik Gateway | 8080 | 8080 |
| Traefik Dashboard | 8081 | 8081 |
| Agent Service | 8000 | 8000 |
| Auth Service | 8001 | 8001 |
| Conversation Service | 8002 | 8002 |
| Paper Storage Service | 8003 | 8003 |
| Vector Search Service | 8004 | 8004 |
| Literature Search Service | 8005 | 8005 |
| Chat Service | 8006 | 8006 |
| Mindmap Service | 8007 | 8007 |
| Analysis Service | 8008 | 8008 |
| MySQL | 3306 | 3306 |
| MinIO API | 9000 | 9000 |
| MinIO Console | 9001 | 9001 |
| Milvus | 19530 | 19530 |
| Attu | 3000 | 9002 |
| Redis | 6379 | 6379 |
| RabbitMQ | 5672 | 5672 |
| RabbitMQ Management | 15672 | 15672 |

## 故障排除

### 服务启动失败

```bash
# 查看具体错误
docker-compose logs <service-name>

# 检查依赖服务是否就绪
docker-compose ps
```

### 数据库连接失败

等待 MySQL 完全启动（通常需要 30-60 秒）：

```bash
docker-compose logs mysql | grep "ready for connections"
```

### OpenAI 连接失败

检查 `.env` 文件中的 `OPENAI_API_KEY` 是否正确配置。

如果需要代理：
```bash
OPENAI_BASE_URL=https://your-proxy.com/v1
```

### 内存不足

Milvus 需要较多内存，建议至少 8GB：

```bash
# 检查 Docker 资源限制
docker stats
```
